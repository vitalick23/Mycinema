@model string

@{ 
    <h3>Ip history</h3>
    string str = "";
    for (int i =0; i < Model.Length; i++)
    {
        if(Model[i] != '|')
        {
            str += Model[i];
        }
        else
        {
            <h4>@str</h4>
            str = "";
        }

    }
}
